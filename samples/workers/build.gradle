plugins {
    id 'kotlin-multiplatform'
}

kotlin {
    targets {
        fromPreset(defaultHostPreset(this), 'workers') {
            compilations.main.outputKinds('EXECUTABLE')
            compilations.main.entryPoint = 'sample.workers.main'
        }
    }
}

createRunTask(this, 'runProgram', kotlin.targets.workers)
